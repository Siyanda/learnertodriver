<nav role="navigation" class="dark-bg full-width flex spaced-between top-nav">

  <!-- menu - logo -->
  <div class="flex nav-area">
    <div class="clickable flex icon">

      <div class="margin-sides lightly padded desktop-only light-fill medium-svg menu icon" data-action="click->sidebar#toggle">
        <%= link_to root_path, aria: { label: root_path("home") } do %>
          <%= inline_svg("logo_icon.svg") %>
        <% end %>
      </div>

      <div class="flex mobile-only tablet-only center-aligned clickable toggle">
        <div class="lightly padded margin-sides menu standard-svg icon" data-action="click->sidebar#toggle">
          <%= inline_svg("icons/menu.svg") %>
        </div>
      </div>

    </div>
  </div>

  <div class="flex nav-area">
    <div class="lightly padded mobile-only tablet-only light-fill medium-svg menu icon" data-action="click->sidebar#toggle">
      <%= link_to root_path, aria: { label: root_path("home") } do %>
        <%= inline_svg("logo_icon.svg") %>
      <% end %>
    </div>
      <!-- large screen nav -->
    <ul class="flex auto-marg navlist desktop-only">
      <li class="flex link <%= class_names(active: current_page?(dashboard_path)) %>">
        <%= link_to "Dashboard", dashboard_path, class: "white-text padded" %>
      </li>
      <li class="flex link <%= class_names(active: current_page?(quizzes_path)) %>">
        <%= link_to "Quizz", quizzes_path, class: "white-text padded" %>
      </li>
      <li class="flex link <%= class_names(active: current_page?(posts_path)) %>">
        <%= link_to "Blog", posts_path, class: "white-text padded" %>
      </li>
      <li class="flex link <%= class_names(active: current_page?(page_path("about"))) %>">
        <%= link_to "About", page_path("about"), class: "white-text padded" %>
      </li>
    </ul>
  </div>

  <div class="flex nav-area">
    <!-- search -->
    <div class="flex center-aligned search-container desktop-only">
      <div class="dark-bg search-wrapper">
        <input type="text" class="search-input" placeholder=" search">
        <div class="svg search-icon icon">
          <%= inline_svg("icons/magnifying-glass.svg") %>
        </div>
      </div>
    </div>

    <div class="flex mobile-only tablet-only center-aligned clickable toggle">
      <div class="lightly padded menu light-stroke standard-svg icon" data-action="click->sidebar#toggle">
        <%= inline_svg("icons/search.svg") %>
      </div>
    </div>
  </div>

</nav>
