<% if current_user.admin? %>

  <div class="admin-options">
    <div class="flex lightly top-padded bottom-padded center-aligned post-title">

      <%= link_to new_page_path, class: 'flex center-aligned rounded dark' do %>
        <div class="mdm-svg icon">
          <%= inline_svg("icons/plus-circle-o.svg") %>
        </div>
        <span class="margin-sides">New Page</span>
      <% end %>

      <%= link_to new_page_path, class: 'flex center-aligned rounded dark' do %>
        <div class="mdm-svg icon">
          <%= inline_svg("icons/plus-circle-o.svg") %>
        </div>
        <span class="margin-sides">Edit Page</span>
      <% end %>

      <%= link_to new_page_path, class: 'flex center-aligned rounded dark' do %>
        <div class="mdm-svg red-text icon">
          <%= inline_svg("icons/trash-o.svg") %>
        </div>
        <span class="margin-sides red-text">Delete Page</span>
      <% end %>

    </div>
  </div>

<% end %>
