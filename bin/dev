#!/usr/bin/env bash

bundle install
bun install

PORT="${PORT:-3000}"
export PORT

if command -v overmind &> /dev/null; then
  overmind start -f Procfile.dev "$@"
else
  foreman start -f Procfile.dev "$@"
fi
